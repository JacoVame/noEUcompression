# noEUcompression
Semantic compression experiment in hyperbolic space: a complete pipeline (co-occurrence → projection → Poincaré → clustering → auto-thresholding) to show how AI can multiply research, if guided with sound judgment.
