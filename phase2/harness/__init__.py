"""Phase-1 eval harness — FROZEN after Phase 1.

Builder sessions (Phases 2-3) must not modify anything in this package; they
create modules under `embeddings/` and call the harness as a black box.
See README-fase2.md, "Standing rules".
"""
